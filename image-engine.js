// UI-independent renderer contract: original image + immutable parameters + resolution.
// rgba16float GPU intermediates are available; legacy parity still quantizes each stage.
const ImageEngine={async render(source,settings,max=0,original=false,backend='auto',precise=null){settings={...Imaging.defaults,...settings};if(settings.precision==='float32'&&!settings.previewGate)return FloatEngine.render(source,precise,settings,max,original,backend);if(backend!=='cpu'){try{const gpu=await GPUImageEngine.render(source,settings,max,original);if(gpu)return {canvas:gpu,backend:'webgpu'};}catch{GPUImageEngine.disable();}}return {canvas:Imaging.render(source,settings,max,original),backend:'cpu-worker'};}};
