(()=>{const button=document.createElement('button');button.id='appUpdates';button.textContent='Updates';button.title='App updates and library restore';document.querySelector('.header-actions').prepend(button);const dialog=document.createElement('dialog');dialog.id='updatesDialog';dialog.innerHTML='<form method="dialog"><div class="dialog-top"><strong>Lith updates</strong><button aria-label="Close updates">✕</button></div><p id="updateVersion" class="small muted"></p><p id="updateMessage"></p><button type="button" id="checkUpdates">Check for updates</button><button type="button" class="primary" id="installUpdate" hidden>Save & restart to update</button><button type="button" id="releasePage">Open downloads</button><hr><p class="small muted">Moving from the portable version? Close it first, then copy its library into this empty installation. Originals and saved looks stay intact.</p><button type="button" id="restoreLibrary">Import existing library</button></form>';document.body.append(dialog);function show(s){$('updateVersion').textContent='Installed version '+s.version;$('updateMessage').textContent=s.message;$('installUpdate').hidden=s.status!=='ready';$('checkUpdates').disabled=['checking','downloading','manual'].includes(s.status);button.textContent=s.status==='ready'?'Update ready':'Updates';}api.onUpdate(show);button.onclick=async()=>{show(await api.updateStatus());dialog.showModal();};$('checkUpdates').onclick=()=>api.checkUpdates().then(show).catch(fail);$('installUpdate').onclick=()=>{ $('installUpdate').disabled=true;api.installUpdate().catch(e=>{fail(e);$('installUpdate').disabled=false;});};$('releasePage').onclick=()=>api.releasePage().catch(fail);$('restoreLibrary').onclick=()=>api.importLibrary().catch(fail);api.updateStatus().then(show).catch(fail);})();
